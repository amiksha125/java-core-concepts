package Interface;

public class Main {
    // Interface is the solution to multiple inheritance in Java
    // class is a blueprint for object
    // interface is a blueprint for class ( skeleton of creating a class )
    // abstract class has abstract methods, and this class specifies all the member functions that a derived class needs to have
    // abstract methods can have concrete methods(regular methods), but interface cannot
    //Interface is a blueprint for class that can only have abstract method and static constant.

    public static void main(String[] args) {

    }
}
